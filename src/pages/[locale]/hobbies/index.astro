---
import HobbiesIndexPage from '@/components/pages/HobbiesIndexPage.astro';
import { localeStaticPaths } from '@/lib/astro/paths';

export function getStaticPaths() {
  return localeStaticPaths();
}
---

<HobbiesIndexPage routeLocale={Astro.params.locale || 'ja-JP'} />
