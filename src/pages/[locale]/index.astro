---
import HomePage from '@/components/pages/HomePage.astro';
import { localeStaticPaths } from '@/lib/astro/paths';

export function getStaticPaths() {
  return localeStaticPaths();
}
---

<HomePage routeLocale={Astro.params.locale || 'ja-JP'} />
