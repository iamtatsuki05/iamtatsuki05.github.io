---
import BlogsIndexPage from '@/components/pages/BlogsIndexPage.astro';
import { localeStaticPaths } from '@/lib/astro/paths';

export function getStaticPaths() {
  return localeStaticPaths();
}
---

<BlogsIndexPage routeLocale={Astro.params.locale || 'ja-JP'} />
