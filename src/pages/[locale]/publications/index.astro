---
import PublicationsIndexPage from '@/components/pages/PublicationsIndexPage.astro';
import { localeStaticPaths } from '@/lib/astro/paths';

export function getStaticPaths() {
  return localeStaticPaths();
}
---

<PublicationsIndexPage routeLocale={Astro.params.locale || 'ja-JP'} />
