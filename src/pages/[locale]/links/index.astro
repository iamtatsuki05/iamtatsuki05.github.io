---
import LinksIndexPage from '@/components/pages/LinksIndexPage.astro';
import { localeStaticPaths } from '@/lib/astro/paths';

export function getStaticPaths() {
  return localeStaticPaths();
}
---

<LinksIndexPage routeLocale={Astro.params.locale || 'ja-JP'} />
